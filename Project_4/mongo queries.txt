1.search hotel on the basis of property name:-
db.cleartrip.find({property_name:"Hotel Jivan Jyoti"},{_id:0})
2.search hotel on the basis of locality:-
db.cleartrip.find({locality:"Paota"}).count()
3.search hotel on the basis of area:- 
db.cleartrip.find(area:"Paota")
4.search hotel on the basis of city:-
db.cleartrip.find({city:"Jodhpur"})
5.search hotel on the basis of property address-
db.cleartrip.find({address:"High Court Colony, Jodhpur. Rajasthan (India)."})
6.search hotel on the basis of room type:-
db.cleartrip.find({room_type:"Business Room"})
7. Search hotel on the basis of city and hotel name:-
db.cleartrip.aggregate([
{$match:

{
	$and:
	[{City:
	{$in:
	["Jodhpur"]
	}}
	,	
	{property_name:"Hotel Jivan Jyoti"}
	]
	}
	},
	{$project:{_id:0,property_name:1,room_type:1}}
	])
8.search on the basis of room type and city:-
db.cleartrip.aggregate([
{$match:

{
	$and:
	[{City:
	{$in:
	["Jodhpur"]
	}}
	,	
	{room_type:"Business Room"}
	]
	}
	},
	{$project:{_id:0,property_name:1,room_type:1}}
	])
9.search on the basis of property name and room type:-
db.cleartrip.aggregate([
{$match:

{
	$and:
	[{property_name:
	{$in:
	["Hotel Jivan Jyoti"]
	}}
	,	
	{room_type:"Business Room"}
	]
	}
	},
	{$project:{_id:0,property_name:1,room_type:1}}
	])
10.search on the basis of property name , room type and city:-
db.cleartrip.aggregate([
{$match:

{
	$and:
	[{property_name:
	{$in:
	["Hotel Jivan Jyoti"]
	}}
	,	
	{room_type:"Business Room"}
	,
	{City:"Jodhpur"}
	]
	}
	},
	{$project:{_id:0,property_name:1,room_type:1}}
	])